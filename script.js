/* 
let baseSquare = 5

function addColorBox(){
    let createLine = document.createElement('div')
    createLine.innerHTML = 'teste';
    let getElementBox = document.querySelectorAll('.color');
    for (let index = 0; index <= getElementBox.length; index += 1) {
        getElementBox[index].appendChild(createLine);
    }
}
addColorBox();
*/
